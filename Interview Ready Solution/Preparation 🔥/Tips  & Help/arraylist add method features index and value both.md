```java
list.add(index,value);
so index if duplicate again found then its right shift first index.

list.add(1,10);
list.add(1,20);
so value will be  [20,10];
right shifting happen when duplicate index again found.
previous value right shift. and new value inserted that index.
```
